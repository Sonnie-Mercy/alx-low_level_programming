#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10 from 0 to 9
 * Return: Always 0
 */
int main(void)
{
	int n;

	for (n = o; n < 10; n++)
		printf("%d", n);

	printf("\n");

	return (0);
}
